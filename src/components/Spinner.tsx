import { cn } from "@/lib/utils";

const Spinner = () => {
  return (
    <span className={cn("size-4 animate-spin border-2 border-white border-t-transparent rounded-full")} />
  );
};
export default Spinner;
                        